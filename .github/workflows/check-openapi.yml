name: Check OpenAPI drift

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: "17 9 * * 1"

permissions:
  contents: read

jobs:
  check-openapi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Compare vendored spec to live spec
        run: node typescript/scripts/check-openapi-drift.mjs

      - name: Check guide-only contract markers
        run: node typescript/scripts/check-docs-contract.mjs
